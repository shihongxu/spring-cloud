server:
  port: 5000
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${user.dir}\src\main\resources\configs
management:
  endpoints:
    web:
      exposure:
        include: "*"
